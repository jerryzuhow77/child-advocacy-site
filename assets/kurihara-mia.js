(()=>{document.querySelectorAll('[data-km-calendar]').forEach(x=>x.innerHTML='<i></i>'.repeat(50));const opening=document.querySelector('.km-opening'),skip=document.querySelectorAll('[data-km-skip]'),replay=document.querySelector('.km-replay');const close=()=>opening&&opening.classList.add('is-gone');skip.forEach(x=>x.addEventListener('click',close));replay&&replay.addEventListener('click',()=>opening&&opening.classList.remove('is-gone'));if(!matchMedia('(prefers-reduced-motion: reduce)').matches)setTimeout(close,22000);const io=new IntersectionObserver(es=>es.forEach(e=>e.isIntersecting&&e.target.classList.add('is-seen')),{threshold:.25});document.querySelectorAll('.km-section').forEach(x=>io.observe(x));})();
